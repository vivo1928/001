# 更新日志

## 均衡器

- 均衡器状态持久化到 SharedPreferences，重启后自动恢复
- `enabled` 改为 `volatile` 保证跨线程可见性，读方法加同步避免数据竞争

## 歌手详情 (Singer Detail)

- 新增歌手简介独立阅读页（公众号风格）
- 酷我源补全歌手简介
- 酷我源新增歌手歌曲列表接口，替代搜索降级
- 酷我源过滤资讯站/专访/纯伴奏等非歌曲杂质
- 跨源兜底保证所有歌手简介完整
- tx 源歌手简介改用开源 GetSingerDetail(wiki) 接口，失败降级旧接口
- kw 源恢复 getToken/tokenRequest 鉴权机制，优先获取最新歌手简介
- 恢复歌手简介独立阅读页 (SingerIntro)
- 打通歌手简介获取链路：本源优先 + 跨源兜底补齐各源简介
- 修复跨源兜底路径 bug，并行拉取所有源取最长简介
- 升级五源歌手简介 API 获取最新数据

## 下载功能

- 下载进度精确到 1% (progressDivider 10→1)
- 修复取消下载弹窗问题

## 无障碍 (Accessibility)

- 修复歌曲列表多选手势及横屏菜单无障碍朗读问题
- 全面优化屏幕阅读器焦点跟手度
- 搜索建议列表用稳定 key，音乐列表主区显式 accessible
- 修复屏幕阅读器下歌曲列表空焦点并对齐排行榜布局
- 搜索歌曲列表使用 `rowType='medium'` 对齐排行榜布局，横屏下间距紧凑且列数逻辑一致
- 修复列表底部状态（加载中/到底了）读屏被识别为按钮的问题，仅错误状态保留按钮角色
- 搜索列表加载逻辑对齐排行榜：错误时清除脏数据，`handleLoadMore` 直接读取 store 最新状态
- 进度滑块改用 `@react-native-community/slider`（底层 `AppCompatSeekBar`）替换自定义 PanResponder+RN a11y 方案。Slider 透明覆盖在视觉进度条上，原生 SeekBar 同步处理触控和 TalkBack 调节，边界音效由系统原生判定（仅 0%/100% 触发），无异步延迟问题
- 进度滑块回到自定义 View + `accessibilityRole="adjustable"` 方案（已验证 TalkBack 手势可用），移除 `announceForAccessibility`，`accessibilityValue` 纯 `{now, min, max}` 无 text，`accessibilityLabel` 固定为"播放进度"（不含百分比），消除双值播报；边界音效由 TalkBack 基于 RangeInfo 原生判定，本地 a11y 值立即更新 + pending 防外部回跳
- 迷你播放器滑块（`Progress.tsx`）、竖屏播放详情滑块（`ProgressBar.tsx`）同步修复，横屏复用 `Progress.tsx`
- 通用滑块（音量/倍速/歌词字号/均衡器等）自定义 View 同步修复：移除 `announceForAccessibility`，`accessibilityValue` 纯 `{now, min, max}` 无 text，`accessibilityLabel` 保持用途描述

## 其他

- 恢复 .so 压缩打包，减小 APK 体积 (useLegacyPackaging)